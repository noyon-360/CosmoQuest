import 'package:flutter/material.dart';

class SpaceAdventureGameScreen extends StatelessWidget {
  const SpaceAdventureGameScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}

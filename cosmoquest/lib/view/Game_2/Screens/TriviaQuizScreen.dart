import 'package:flutter/material.dart';

class TriviaQuizScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Trivia Quiz")),
      body: Center(
        child: Text("Trivia Quiz Game"),
      ),
    );
  }
}
